import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: {
    template: '%s - AI Embedding Document Search Engine',
    default: 'AI Embedding Document Search Engine',
    absolute: 'AI Embedding Document Search Engine',
  },
  description: 'Markdown AI Embedding Document Search Engine',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
